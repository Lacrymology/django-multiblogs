.. _installation:

Installation
============

* To install ::

    pip install django-multiblogs

* Add ``'multiblogs'`` to your ``INSTALLED_APPS`` setting::

    INSTALLED_APPS = (
        # other apps
        "multiblogs",
    )

